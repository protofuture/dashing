<li>
  <%= link_to user.name, user %> | <%= link_to "delete", user, :method => :delete,
                                                               :confirm => "Are you sure?",
                                                               :title => "Delete #{user.name}" %>
</li>
